// Configure where the frontend should call the backend.
//
// This repo uses a single Lambda Function URL (no API Gateway).
//
// Example:
//   window.CAT_UPLOAD_CONFIG = {
//     apiBaseUrl: "https://xxxxx.lambda-url.ap-southeast-1.on.aws/"
//   };
//
window.CAT_UPLOAD_CONFIG = {
    // apiBaseUrl: "YOUR LAMBDA FUNCTION URL"
}